# Filter examples

The former automatically loaded `process_log` scriptlets are retired.
Use `-script examples/operations.star`; see [LOGSCRIPT.md](../LOGSCRIPT.md).
The example below selects already enriched logs; enrichment must occur upstream.
